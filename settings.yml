use_default_settings:
  engines:
    keep_only:
      - google
      - duckduckgo
      - brave
      - wikipedia

server:
  image_proxy: true

outgoing:
  request_timeout: 5.0
  max_request_timeout: 10.0
  source_ips:
    - "0.0.0.0"
  pool_connections: 100
  pool_maxsize: 20
